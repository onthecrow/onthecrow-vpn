<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keep the VPN tunnel alive through Doze (request exemption at runtime). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Recovery holds a PARTIAL_WAKE_LOCK across the whole re-dial so the CPU cannot suspend
         half-way through re-establishing the tunnel. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Restore the tunnel after a reboot / app update if it was up when we lost it. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Fallback only. The battery-optimization allowlist already grants exact alarms
         (AlarmManagerService#isExemptFromExactAlarmPermissionNoLock); this covers the user later
         revoking that exemption. Deliberately NOT USE_EXACT_ALARM, which Play restricts to
         alarm-clock/calendar/timer apps. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".OnthecrowVpnApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Long-pressing the Quick Settings tile opens this activity. -->
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile: tap toggles the tunnel with the selected config. Uses a dedicated
             full-bleed vector rather than the adaptive launcher foreground, whose safe-zone margin made
             the tile render smaller than every other app's. -->
        <service
            android:name=".VpnTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_tile"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- android:process=":vpn" is load-bearing: the tunnel is restored without ever cold-starting
             the main process. The recovery restart itself uses no receiver at all — its PendingIntent
             targets the service directly. -->
        <receiver
            android:name=".vpn.BootRestoreReceiver"
            android:directBootAware="false"
            android:exported="false"
            android:process=":vpn">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".vpn.OnthecrowVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":vpn">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>

            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>
    </application>

</manifest>